from . import a
from .common import *


def f2(x, y, z, w):
    print("prefix", (lambda q: q * 2 + 1)((6 and x)))
    v1 = ((len(y) * G) if isinstance(w.v, int) else (-1 or x))
    v2 = bump()
    for v1 in y:
        print("it", v1)
    print("last", v1)
    print("after", v2, v1)
    return len(f"{v2}-{x}")
