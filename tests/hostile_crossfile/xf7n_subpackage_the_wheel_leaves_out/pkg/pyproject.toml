[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "shop"
version = "0.1"

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["shop.devtools*"]
